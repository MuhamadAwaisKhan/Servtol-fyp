import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  static const grey = Color(0x00FFA49999);
  static const black = Color(0x00C0000000);
  static const green = Color(0x00FF13CB06);
  // static const customButton = Color(0x00FF1B1BD9);
  static const background = Color(0xFFCAF0F8); // Light blue background
  static const primaryColor = Color(0xFF4A90E2); // Primary blue color
  static const accentColor = Color(0xFFFF6F61); // Accent coral color
  static const primaryTextColor = Color(0xFF333333); // Dark gray for primary text
  static const secondaryTextColor = Color(0xFF7A7A7A); // Gray for secondary text
  static const category = Color(0x00004385);
  static const heading = Color(0x00FF023E8A );
  static const customButton = Color(0x00FF03045E );
  // static const Color primaryColor = Colors.deepPurple;  // Use a shade like deepPurple[700] if needed
  static const Color secondaryColor = Colors.lightBlueAccent; // Use a specific shade like tealAccent[400] for more vibrance
  static const Color iconColor = Colors.white; // Common choice for icons for readability against dark colors
  static const Color heading12 = Colors.white; // Good contrast on dark backgrounds
  static const Color background12 = Colors.grey; // Soft background that won't clash with deepPurple


}
